# API Reference

The API reference is generated from the installed package, so signatures and
docstrings stay synchronized with the implementation.

```{toctree}
:maxdepth: 1

priors
linear-models
tree-models
neural-network
gaussian-process
```
